/**
 * Role feature actions export
 */

export { getRoles, getRoleByIdAction, createRole, updateRole, deleteRole, getModules, getPermissions, assignPermissionToRole, removePermissionFromRole } from "./role-actions";
