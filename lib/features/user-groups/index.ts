/**
 * User Groups Feature - Main export
 * Centralized export point for all user groups feature modules
 */

// Types
export * from "./types";

// Actions (Server)
export * from "./actions";

// Services (Internal - not exported to avoid loading server code in client)
// Services are only used within Server Actions
// Repositories (Internal - not exported)
